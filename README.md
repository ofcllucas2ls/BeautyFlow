
  # BeautyFlow

  This is a code bundle for BeautyFlow. The original project is available at https://www.figma.com/design/nNc9WLF6xgX0CC9drZF0h2/BeautyFlow.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  